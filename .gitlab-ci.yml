#redis/.gitlab-ci.yml
stages:
  - build_and_deploy

build_and_deploy:
  stage: build_and_deploy
  image: docker:latest

  script:
    - cd ./deployments/

    - cp "$REDIS_ENV" .env

    - apk add --no-cache bash
    - bash ./deploy.sh
  only:
    - main
  tags:
    - redis
