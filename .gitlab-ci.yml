#redis/.gitlab-ci.yml
stages:
  - build_and_deploy

build_and_deploy:
  stage: build_and_deploy
  image: docker:latest

  script:
    - echo "Авто-деплой отключен из-за неверно работающего GitLab-раннера."
    - echo "Вместо файла раннер создает папку"
    - echo "Сейчас используется ручное стягивание и запуск"
#    - cp "$REDIS_ENV" .env

#    - apk add --no-cache bash
#    - bash ./run.sh
  only:
    - main
  tags:
    - redis
